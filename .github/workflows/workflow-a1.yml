name: 🔄 A1 Workflow

on:
  repository_dispatch:
    types: [run_A1]
  workflow_dispatch: {}  # Manual start for first run

permissions:
  actions: write
  contents: write

jobs:
  a1-job:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: 🚀 A1 Running
        run: |
          echo "⏳ A1 started at $(date)"
          # Your 8 min work here
          sleep 480  # 8 minutes
          echo "✅ A1 finished at $(date)"

      - name: ➡️ Trigger A2
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            await github.rest.repos.createDispatchEvent({
              owner: context.repo.owner,
              repo: context.repo.repo,
              event_type: 'run_A2'
            });
            console.log('✅ Triggered A2');
