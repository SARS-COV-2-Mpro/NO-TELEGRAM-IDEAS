name: 🔄 A3 (8 min)

on:
  repository_dispatch:
    types: [run_A3]   # <--- MUST MATCH WHAT WORKER SENDS

permissions:
  actions: write
  contents: write

jobs:
  a3:
    runs-on: ubuntu-latest
    timeout-minutes: 8

    steps:
      - name: 🚀 A3 Work
        run: |
          echo "⏳ A3 started at $(date)"
          sleep 180  # Sleeps 8 minutes
          echo "✅ A3 done at $(date)"

      # This step triggers the Ideas Pusher (Code B)
      - name: 🔁 Trigger Ideas Pusher
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            await github.rest.repos.createDispatchEvent({
              owner: context.repo.owner,
              repo: context.repo.repo,
              event_type: 'run_B'
            });
            console.log('🅱️ Triggered Ideas Pusher');
